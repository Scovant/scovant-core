"""Analysis over already-fetched content: metadata-integrity screening for
MCP tool descriptions, static per-tool risk classification, and agent
instruction reference integrity (extraction plus best-effort resolution)."""
